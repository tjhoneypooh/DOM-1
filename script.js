function main() {

    // const is used because you don't want to reassign

    // add the "big" class to the "grow me" paragraph
    // const grow = document.getElementById("grow-me");
    // grow.classList.add("big");

    // remove the big class to the shrink me paragraph
    // const shrink= document.getElementById(`shrink-me`);
    // shrink.classList.remove(`big`);

     // find all the li's and log their text content to the console
    // const lis = document.querySelectorAll(`li`);
    // lis.forEach(li => console.log(li.textContent));

    // // set the href link to "https://www.example.com" and update the text to say 
    // // "somewhere" instead of "nowhere"
    // const a = document.querySelector(`.link`);
    // a.setAttribute("href", "https://www.example.com");
    // a.textContent = `somewhere`;

    // Set the display CSS property of the "hide-me" paragraph to "none"
    // const hideMe = document.getElementById("hide-me");
    // hideMe.style.display = "none";

    // Set the display CSS property of the "show-me" paragraph to "clock"
    // const showMe = document.getElementById("show-me");
    // showMe.style.display = "block";

    // Get the text that the user enters into the "name" input box and use it to set
    // a welcome message in the <h1>, eg. "Hello Grant!"
    // const welcome = document.querySelector('h1');
    // const input = document.getElementById(`name`);
    // welcome.textContent = `Welcome ${input.value}`;
    
}